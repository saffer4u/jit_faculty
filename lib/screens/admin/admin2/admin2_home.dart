import 'package:flutter/material.dart';

class Admin2Home extends StatelessWidget {
  static const routeName = 'admin2Home';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Admin2 Home'),
      ),
      body: Center(
        child: Text('Admin2Home Page'),
      ),
    );
  }
}
